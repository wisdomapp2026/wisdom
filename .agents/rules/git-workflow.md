# Git Workflow Rules for Wisdom Project

## Repository Details
- **Account:** wisdomapp2026
- **Repository:** https://github.com/wisdomapp2026/wisdom.git
- **Main Branch:** main
- **Credential Storage:** Windows Credential Manager (`git:https://github.com`) configured for user `wisdomapp2026`.

## Git Push Instructions
- When making commits or pushing to remote:
  1. Verify `.env*` files are never tracked or committed.
  2. The remote `origin` is always `https://github.com/wisdomapp2026/wisdom.git`.
  3. Git pushes automatically use stored credentials in Windows Credential Manager.
